# 2023-web-study

## Starting Github

> install git

`$ sudo apt install git`
or
`$ sudo apt install -y git`

> check version

`$ git --version`

### Clone remote repository
`$ git clone <repository-remote>`
